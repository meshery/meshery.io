name: Kanvas Screenshot Service
on: # rebuild any PRs and main branch changes
  pull_request_target:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      contentID: 
        description: ID of the design to render.
        required: true
        type: string
      assetLocation:
        required: true
        type: string
        description: Remote location where the generated asset (screenshot) for the design will be stored.
  workflow_call:
    inputs:
      contentID: 
        description: ID of the design to render.
        required: true
        type: string
      assetLocation:
        required: true
        type: string
        description: Remote location where the generated asset (screenshot) for the design will be stored.
    outputs:
      resource_url:
        description: "The URL of the generated resource."
        value: ${{ jobs.KanvasScreenshot.outputs.resource_url }}
permissions: 
  actions: read
  contents: write
  security-events: write
  statuses: write
  pull-requests: write
  id-token: write

jobs:
  KanvasScreenshot:
    runs-on: ubuntu-24.04
    outputs:
      resource_url: ${{ steps.test_result.outputs.resource_url }}
    steps:
      - name: Set PR number # To comment the final status on the Pull-request opened in any repository
        run: |
          export pull_number=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")
          echo "PULL_NO=$pull_number" >> $GITHUB_ENV
      - uses: actions/checkout@v4 #this step would go away
        with:
          path: action
          repository: layer5labs/kanvas-snapshot
      - run: |
         echo ${{ inputs.contentID }}
         echo ${{ inputs.assetLocation }}
        shell: bash
      - name: Wait for Meshery Playground
        run: |
          max_attempts=30
          attempt=0
          while [ $attempt -lt $max_attempts ]; do
            if curl -s -o /dev/null -w "%{http_code}" https://playground.meshery.io | grep -q "200\|301\|302"; then
              echo "Meshery Playground is ready"
              exit 0
            fi
            echo "Waiting for Meshery Playground... (attempt $((attempt+1))/$max_attempts)"
            sleep 5
            ((attempt++))
          done
          echo "Warning: Meshery Playground may not be ready, but proceeding with test"
      - name: Verify Meshery Token
        run: |
          if [ -z "${{ secrets.MESHERY_TOKEN }}" ]; then
            echo "⚠️  WARNING: MESHERY_TOKEN is not set in repository secrets"
            echo "Tests may fail if token is required for design access"
          else
            echo "✓ MESHERY_TOKEN is configured"
          fi
      - name: Display Test Configuration
        run: |
          echo "=== Cypress Test Configuration ==="
          echo "Base URL: https://playground.meshery.io"
          echo "Chrome WebSecurity: false"
          echo "Design ID: ${{ inputs.contentID }}"
          echo "Asset Location: ${{ inputs.assetLocation }}"
          echo "PR Number: ${{ env.PULL_NO }}"
          echo "================================"
      - id: test_result
        uses: layer5labs/Kanvas-Snapshot@v0.2.41
        continue-on-error: true
        with:
          githubToken: ${{ secrets.GITHUB_TOKEN }} # github's personal access token example: "ghp_...."
          mesheryToken: ${{ secrets.MESHERY_TOKEN }} # Meshery Cloud Authentication token, signin to meshery-cloud to get one, example: ey.....
          prNumber: ${{ env.PULL_NO }} # auto-filled from the above step
          application_type: "Design" # your application type, could be any of three: "Kubernetes Manifest", "Docker Compose", "Helm Chart"
          skipComment: true
          designID: ${{ inputs.contentID }} # relative file-path from the root directory in the github-runner env, you might require to checkout the repository as described in step 2
          assetLocation: ${{ inputs.assetLocation }}
        env:
          CYPRESS_chromeWebSecurity: false
          CYPRESS_RECORD_DISABLED: true
          CYPRESS_baseUrl: "https://playground.meshery.io"
          CYPRESS_viewportWidth: 1920
          CYPRESS_viewportHeight: 1080
          CYPRESS_defaultCommandTimeout: 20000
          CYPRESS_requestTimeout: 30000
          CYPRESS_responseTimeout: 45000
      - name: Check Test Result
        if: always()
        run: |
          if [ "${{ steps.test_result.outcome }}" == "failure" ]; then
            echo "⚠️  Cypress tests failed, but artifacts were generated"
            echo "Download screenshots and videos from the workflow artifacts to debug"
          else
            echo "✓ Tests completed successfully"
          fi
