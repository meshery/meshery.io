/**
 * Catalog modal - refactored with separation of concerns
 */

import { initReadMore } from '../../src/components/readMoreHandler.js';

document.addEventListener('DOMContentLoaded', function() {
  initReadMore();
});
